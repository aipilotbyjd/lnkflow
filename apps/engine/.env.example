# LinkFlow Go Engine - Environment Configuration
# Copy to .env and customize for your environment

# ============================================
# Database
# ============================================
POSTGRES_USER=linkflow
POSTGRES_PASSWORD=linkflow
POSTGRES_DB=linkflow

# ============================================
# Logging
# ============================================
LOG_LEVEL=info
# Options: debug, info, warn, error

# ============================================
# SECURITY - CRITICAL CONFIGURATION
# ============================================
# These secrets MUST be changed in production!
# Generate strong secrets using: openssl rand -base64 32

# JWT secret for API authentication (minimum 32 characters)
# SECURITY: This secret is used to sign and verify JWT tokens
# A weak secret allows attackers to forge authentication tokens
JWT_SECRET=REPLACE_WITH_STRONG_SECRET_MINIMUM_32_CHARS

# Shared secret with Laravel API for callback authentication
# SECURITY: Used to sign callbacks from the engine to Laravel
# Must match the LINKFLOW_SECRET configured in your Laravel app
LINKFLOW_SECRET=REPLACE_WITH_STRONG_SECRET_FOR_CALLBACKS

# ============================================
# Cell Configuration (for multi-region)
# ============================================
CELL_ID=cell-1
REGION=local

# ============================================
# Optional: Elasticsearch for advanced search
# ============================================
# ELASTICSEARCH_URL=http://elasticsearch:9200

# ============================================
# Production Security Checklist
# ============================================
# Before deploying to production, ensure:
# [ ] JWT_SECRET is a cryptographically random 32+ character string
# [ ] LINKFLOW_SECRET is a cryptographically random 32+ character string
# [ ] POSTGRES_PASSWORD is changed from default
# [ ] Database SSL is enabled (sslmode=require or verify-full)
# [ ] All services are behind a reverse proxy with TLS
# [ ] Network policies restrict inter-service communication
# [ ] Secrets are managed via a secret management solution
